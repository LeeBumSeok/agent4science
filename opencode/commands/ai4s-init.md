---
description: Initialize the .ai4science/ research ledger for this project
---

Initialize an AI4Science project in this repository.

The scientific goal is: $ARGUMENTS

Call the `ai4s_scaffold` tool with `goal` set to the goal above (if the goal is empty, call
it with no goal). Then briefly report:

- that `.ai4science/` was created (or already existed) and the current state,
- the recommended next step: run `/ai4s-pro-prompt` to draft a ChatGPT Pro research prompt.

Do not create any other files. Do not start implementing anything yet.
